BUGS -- known bugs. 

* if tab-page is changed during the module's gui constraction, then the page is 
treated as non-editable in menus

* strange behaviour of checkbox widgets for atomic-coordinates (iforce
components)


Fixed BUGS:

* GUI freeze when read a PW input file with an empty namelist (Fixed)

BUGS ends here.
