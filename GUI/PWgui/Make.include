TCL_VERSION      = 8.5
ITCL_VERSION     = 3.4
IWIDGETS_VERSION = 4.0.1

# shortcuts

TCL_VER      = $(TCL_VERSION)
ITCL_VER     = $(ITCL_VERSION)
IWIDGETS_VER = $(IWIDGETS_VERSION)

# where to find those libraries

EXTERNAL_LIB_PREFIX = /usr/lib
