#
# System-dependent Make definitions, sun 
# ( Using sunperf library )
#
# Preprocessor
#
CPP = fpp
CPPFLAGS = -P -DSUN -DNOPENCILS -DHAS_ZHEGVX -I$(INCL_DIR)
#
# Fortran compiler:
#
FC = f90
#
FFLAGS = -fast -dalign -xchip=ultra3 -xarch=v8plusb \
         -M$(INCL_DIR) -xlic_lib=sunperf 
#
LIBS = -L/usr/opt/SUNWhpc/lib -R/opt/SUNWhpc/lib -lmvec 
#
LD=$(FC)
LFLAGS = $(LIBS)
#
# ar:
#
AR = ar
ARFLAGS = ruv
